# comma-controls-challenge
Built a custom controller based on the baseline PID. Added feedforward control, future trajectory preview using future_plan, integral windup protection, and action smoothing/rate limiting to reduce jerk while maintaining tracking accuracy. Evaluated on the full 5000-segment dataset and improved total cost from 110.255 (baseline PID) to 97.096.
